[project]
name = "acta-ergo-sum"
version = "0.1.0"
description = "GitHub activity data extractor — I act, therefore I am."
requires-python = ">=3.12"
dependencies = [
    "typer>=0.9.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[project.scripts]
acta = "acta.cli:app"
